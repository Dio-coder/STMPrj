# 运行期内存与执行模型

链接期把地址排好之后，程序**跑起来**时发生什么：代码从哪取、变量住在哪、PC/SP/LR 各管什么。

> `STMTest/LEARNING.md` 第 3 层讲的是**链接期**的段布局（VMA/LMA、地址怎么算出来）。
> 这份文档接在它后面，讲**运行期**：栈、堆、寄存器、调用与返回。
>
> 本文所有地址和数字都是从 `STM32HAL/build/firmware.elf` **实测**出来的
> （`arm-none-eabi-nm` / `objdump`），不是示意值。目标板 STM32F103C8T6，Flash 64K / RAM 20K。

---

## 零、一张总图

```
Flash 0x08000000 ┌────────────────────┐
                 │ .isr_vector        │ 硬件复位时读头两项（SP、PC）
      0x0800010C │ .text  指令        │ ← PC 从这里取指，原地执行(XIP)，不搬运
                 │   └ 字面量池       │ ← ldr [pc,#N] 取常量 / 静态变量的地址
      0x080012D4 │ .rodata            │ ← 字符串字面量、const 数据
      0x080013C8 │ .data 的初值副本    │ ← Reset_Handler 把这份搬到 RAM
                 └────────────────────┘

RAM   0x20000000 ┌────────────────────┐
                 │ .data     16 B     │ 全局/static，有非零初值  ┐ 静态区 120 B
      0x20000010 │ .bss     104 B     │ 全局/static，零初值      ┘ 链接期钉死
      0x20000078 ├────────────────────┤ ← _ebss / 堆起点
                 │                    │
                 │   空隙  19.9 KB    │ 栈和堆共用，无边界、无仲裁、无保护
                 │                    │
      0x20005000 └────────────────────┘ ← _estack，SP 起点，向下增长
```

实测符号：

```
_sidata = 0x080013C8      _sdata = 0x20000000    _edata = 0x20000010
_sbss   = 0x20000010      _ebss  = 0x20000078    _estack = 0x20005000
静态占用 120 B = 0.6%     空隙 20360 B = 99.4%
```

---

## 一、Flash 里有什么

### 函数在 Flash 里**原地执行**，不搬到 RAM

判据是段的 VMA（运行地址）和 LMA（存储地址）：

| 段 | VMA | LMA | 结论 |
|---|---|---|---|
| `.text` | `0800010C` | `0800010C` | **相等** → 原地使用，不搬 |
| `.rodata` | `080012D4` | `080012D4` | **相等** → 原地使用，不搬 |
| `.data` | `20000000` | `080013C8` | **不等** → Reset_Handler 拷贝 |
| `.bss` | `20000010` | — | Reset_Handler 清零 |

`.text` 的运行地址就是它在 Flash 里的位置，所以不需要任何搬运动作 —— 这叫 **XIP（eXecute In Place）**，Cortex-M3 的取指总线直接连着 Flash。

`Reset_Handler` 全程只干两件事：**拷 `.data`、清 `.bss`**，从不碰 `.text`。

### Flash 里有三类内容

| 内容 | 段 | 例子（实测） |
|---|---|---|
| 指令 | `.text` | `main` @ `08000510` |
| **字面量池** | `.text` 内部 | `0xCCCCCCCD` @ `0800050C`（除以 10 的魔数）、`0x20000000`（静态变量的**地址**本身） |
| 只读数据 | `.rodata` | `"blink "` 等字符串、`const` 数组 |

字面量池这条容易忽略：**静态变量的地址，本身是一个存在 Flash 里的常量。** `clock_source` 住在 RAM `0x20000000`，但「它住在 0x20000000」这个信息是刻在 Flash 里的。

---

## 二、RAM 里有三个区域，不是两个

| 区域 | 谁住这里 | 地址何时确定 | 本工程大小 |
|---|---|---|---|
| **静态区** `.data`+`.bss` | 全局变量、`static` 变量（含 static 局部） | **链接期**钉死 | **120 B** |
| **堆** heap | `malloc()` 返回的内存 | 运行期由分配器决定 | **0 B** |
| **栈** stack | 普通局部变量、调用帧、中断异常帧 | 运行期随 SP 移动 | 动态 |

**初始化的内容进静态区，不是堆。** 堆只装 `malloc` 出来的东西。

### 本工程的堆是 0 —— 有两重证据

1. `malloc` / `free` / `_sbrk` **一个符号都没链接进来**
2. `.ld` 里写的是 `PROVIDE(end = _ebss)`，而 `PROVIDE` 的语义是「只有当有代码引用它、却没人定义时才定义」。实测 **`end` 符号根本没被生成** → 从来没有代码引用过堆起点

裸机项目不用堆是**常态而非缺失**：碎片化无人整理、`malloc` 返回 `NULL` 时无处可去、分配耗时不确定破坏实时性、而且外设句柄和缓冲区大小编译期都已知，静态分配就够了。

### 静态区大小由代码决定，链接期固定，运行期不变

「固定」有两层意思，只有一层对：

| 说法 | 对不对 |
|---|---|
| 静态区**运行期不变** —— 链接完地址和大小就钉死 | ✅ |
| 静态区有个**预设的大小或上限** | ❌ 你写多少全局变量它就多大 |

实测（往代码里加一个全局数组，看静态区和空隙怎么变）：

```
全局数组   0 B  → 静态区     4 B  → 留给栈+堆  20476 B
全局数组  16 KB → 静态区 16388 B  → 留给栈+堆   4092 B
全局数组  24 KB → ❌ 链接失败：region `RAM' overflowed by 4100 bytes
```

### 链接器管一半，不管另一半

| 情况 | 链接器 |
|---|---|
| `.data + .bss` 超过 20K | ✅ **报错**，链接失败 |
| 塞得进，但只剩 100 B 给栈 | ❌ 不管，照样链接成功 |
| 运行期栈往下长、撞进 `.bss` | ❌ 完全管不到 |

**第三种最危险**：Cortex-M3 没配 MPU 时**没有硬件栈边界检查**。栈溢出不会 HardFault、不会告警，只是**静默覆盖全局变量** —— 症状是「某个全局变量的值莫名变了」，且和代码分支相关，极难查。

> ST 官方模板用 `._user_heap_stack` 段在 RAM 里**占位** `_Min_Heap_Size(512) + _Min_Stack_Size(1024)` = 1.5K，
> 把「留给栈的空间」变成链接器看得见的静态占用，这样第二种情况也会报错。
> 本工程的 `.ld` 没有这个段（静态区才占 0.6%，暂时不需要）。
>
> 官方模板参考路径：
> `STM32CubeF1/Projects/STM32F103RB-Nucleo/Templates_LL/SW4STM32/NUCLEO-F103RB/STM32F103RBTx_FLASH.ld`

### 测栈实际用了多少

链接期看不出来（栈是运行期的）。手法是**栈水位标记**：启动时把空隙填成固定模式（如 `0xDEADBEEF`），跑一段时间后用 GDB 从 `_ebss` 往上扫，第一个被改掉的位置就是历史最深水位。

```
(gdb) x/64xw 0x20000078      # 从 _ebss 往上找第一个不是 0xDEADBEEF 的
```

---

## 三、`static` 到底改变了什么

**`static` 决定的是「谁能看见」或「活多久」，从不直接决定「存在哪」。**

| `static` 加在哪 | 改变了什么 | 没改变什么 |
|---|---|---|
| 函数、全局变量 | **可见性** → 仅本文件可见 | 位置（函数还在 Flash，全局变量还在 RAM） |
| **局部变量** | **存储期** → 从栈搬到固定地址，跨调用保持 | 作用域（依然只有那个函数能访问） |

局部变量本来就只有函数内可见，`static` 没法再缩小，所以它改的是**生命周期**。这就是 static 局部变量能累加的原因。

### 实测：函数加不加 static，都在 Flash

| 函数 | 符号 | 地址 | 段 |
|---|---|---|---|
| `static void uart_print` | **`t`** 小写 | `0800047C` | .text → Flash |
| `static void GPIO_Init` | **`t`** 小写 | `080003A4` | .text → Flash |
| `int main` | **`T`** 大写 | `08000510` | .text → Flash |

唯一区别是**符号字母的大小写**：小写 = 局部符号，大写 = 全局符号。地址全在 Flash。

### 实测：static 局部变量的落点

| 声明（函数内） | 编译器给的符号 | 最终地址 | 位置 | 段 |
|---|---|---|---|---|
| `static int b = 7;` | `b.0` | `20000000` | **RAM** | `.data`（从 Flash 拷初值） |
| `static int b = 11;`（另一函数同名） | `b.4` | `20000004` | **RAM** | `.data` |
| `static int c = 0;` | `c.3` | `20000008` | **RAM** | `.bss`（清零） |
| `static int d;` | `d.2` | `2000000C` | **RAM** | `.bss` |
| `static const char tab[4]` | `tab.1` | `08000044` | **Flash** | `.rodata`（不搬） |
| `int a = i;` | 无符号 | — | **栈或寄存器** | 无 |

两个细节：

- **编译器会改名**：两个函数里同名的 `static int b` 变成 `b.0` 和 `b.4`，独立对象、独立地址、不冲突。「文件内可见」在实现上就是靠改名 + 局部符号做到的。
- **`static const` 局部变量不进 RAM**：永不修改 → 留在 `.rodata`(Flash)。这是「static 局部变量会进 RAM」的**唯一例外**。

### 决定进 RAM 还是 Flash 的是「初始化方式」

| 声明 | 段 | 位置 | Reset_Handler |
|---|---|---|---|
| `int x = 5;` / `static int x = 5;` | `.data` | RAM（初值存 Flash） | 拷贝 |
| `int x;` / `static int x;` | `.bss` | RAM | 清零 |
| `const int x = 5;` | `.rodata` | **Flash** | 不管 |
| 函数（无论 static） | `.text` | **Flash** | 不管 |
| 函数内普通局部变量 | 无段 | **栈 / 寄存器** | 不管 |

> **实用技巧**：只读的大查表数组加 `const` 就落在 `.rodata`(Flash)，**完全不吃 20K RAM**。

### 坑：只读的 static 局部变量可能被优化掉

实测把 static 局部变量写成只读不改：

```c
static int b = 7;  static int c = 0;  static int d;
sink = a + b + c + d;
```

`-Og` 下 **四个变量全被删掉**，`.data`/`.bss` 大小都是 0 —— 编译器发现它们从没被修改，直接常量折叠成 `sink = a + 7`。

**`static` 局部变量只有真正需要跨调用保持状态时才会占 RAM。** 所以看实际占用要用 `nm`/`objdump` 看产物，不能只看源码。

---

## 四、四种数据访问方式的指令形态

| 区域 | 指令形态 | 地址从哪来 |
|---|---|---|
| **Flash 取指** | 无显式指令 —— PC 自增，硬件自动抓 | 链接期定死 |
| **静态区** | `ldr r3, [pc, #16]` 取出 `0x20000000` → `str r2, [r3]` | **Flash 里的常量**，链接期算好 |
| **栈** | `sub sp, #28` 开帧 → `strb [sp, #12]` | **SP 相对**，运行期才知道 |
| **堆** | `bl malloc` → 地址从 `r0` 返回 | 运行期分配器决定 |

实测 `SystemClock_Config()` 访问静态变量 `clock_source`：

```
8000384:  ldr r3, [pc, #16]      ← 从字面量池取地址
8000388:  str r2, [r3, #0]       ← 往那个地址写
   ...
8000398:  20000000  .word        ← clock_source 的地址，硬编码在 Flash 里
800039C:  080012D4  .word        ← "HSE 8MHz x9 -> 72MHz" 字符串，在 Flash
```

**静态区和堆的根本区别**：静态区的地址是链接期常量（刻在指令流里），堆的地址是运行期返回值（只存在于寄存器/变量里）。

### 局部变量**不一定**占栈

实测 `uart_print_uint()`：`int i`、`int j`、参数 `v` 在反汇编里**找不到** —— 全程待在 `r0`–`r3` 寄存器，从没碰 RAM。只有 `tmp[11]` 和 `buf[12]` 落到栈上。

**什么时候必须落栈：**

| 情况 | 为什么 |
|---|---|
| **数组** | 要按下标寻址，必须有连续地址空间 |
| 取了地址（`&x`） | 指针必须指向真实内存 |
| 变量太多，寄存器不够 | Cortex-M3 只有 r0–r12 |
| 跨函数调用还要活着 | 调用会破坏 r0–r3 |

准确说法是：**局部变量优先待在寄存器里，装不下或用法不允许时才落栈。** 这也是 `-Og`/`-O2` 下单步调试常看到 `<optimized out>` 的原因。

---

## 五、PC / SP / LR：三个寄存器，三件事

| 寄存器 | 全名 | 存什么 | 指向哪里 |
|---|---|---|---|
| **PC** | Program Counter | **下一条指令的地址** | **Flash** `0x0800xxxx` |
| **SP** | Stack Pointer | **栈顶地址** | **RAM** `0x2000xxxx` |
| **LR** | Link Register | 函数返回地址 | Flash |

> ⚠️ **最常见的混淆：SP 不是指令地址，PC 才是。** SP 指向的是**数据**（栈顶），永远在 RAM 里。

实测 `uart_print_uint()` 的完整栈帧生命周期：

```
8000498:  push {lr}            ← LR(返回地址) 压栈，SP -= 4
800049A:  sub  sp, #28         ← SP -= 28，给 tmp[]/buf[] 开空间
   ...
8000500:  mov  r0, sp          ← 把栈上数组地址当参数传
8000502:  bl   uart_print      ← PC ← 0x800047C，LR ← 0x8000506
8000506:  add  sp, #28         ← SP += 28，回收局部变量
8000508:  ldr.w pc, [sp], #4   ← 从栈弹出返回地址 → 写进 PC，SP += 4
```

最后一条最能说明区别：**从 SP 指的地方读出一个值，装进 PC**。SP 是「去哪读」，PC 是「读出来的东西」。

函数返回时 `add sp, #28` 一条指令就"释放"了 28 字节 —— 没有任何清理代码，这是栈比堆快的根本原因。

### SP 主要由**指令**更新，硬件自动只有两处

| 时机 | 谁动 SP |
|---|---|
| 正常函数调用/返回 | **指令**：`push` / `pop` / `sub sp` / `add sp` |
| **复位** | **硬件**：从向量表第 0 项（Flash `0x08000000`）读 `_estack` 装进 SP |
| **进/出异常** | **硬件**：自动压 8 个字（r0–r3, r12, LR, PC, xPSR），SP -= 32；返回时弹回 |

第三条以后做中断时很要紧：**每次中断进来，硬件先吃掉 32 字节栈。** 中断嵌套深了栈消耗会翻倍。

---

## 六、函数调用与返回

### 函数执行完，PC 回到**调用者**，不是「下一个函数」

实测 `main()` 开头：

```
8000510 <main>:
 8000510:  push {r3, r4, r5, lr}        ← main 自己也要 bl，得先存 LR
 8000512:  bl 8000618 <HAL_Init>        ← PC ← 0x8000618, LR ← 0x8000516
 8000516:  bl 800037C <SystemClock_Config>
 800051A:  bl 80003A4 <GPIO_Init>
 800051E:  bl 80003EC <UART1_Init>
```

`HAL_Init` 执行完，PC **回到 `0x8000516`** —— 是 `main` 里 `bl` 的下一条，不是 `SystemClock_Config` 的入口。回到 `main` 后，`main` 才自己发出下一条 `bl`：

```
main ──bl──> HAL_Init
main <──返回── (PC = 0x8000516)
main ──bl──> SystemClock_Config
main <──返回── (PC = 0x800051A)
     ...
```

**每次都绕回 `main`。函数之间从不直接相连。** 这就是调用/返回纪律，也是需要 LR 和栈的原因 —— 被调函数必须知道回哪儿去，嵌套调用靠栈层层保存返回地址。

### `bl` 只改 PC 和 LR，不动 SP

```
8000502:  bl uart_print
```

`bl`（Branch with Link）：**PC ← 目标地址**，**LR ← 下一条指令地址**。**SP 完全没动。**

SP 只在被调函数**自己需要栈空间**时才移动。一个所有变量都在寄存器、又不调用别人的函数，可以一次都不碰 SP。

正确的表述：

```
bl func   →  PC = func 地址（跳转）、LR = 返回地址
          →  进入 func：若需要栈，它自己 push {lr} / sub sp, #N   ← 这才动 SP
          →  返回：add sp, #N / pop {pc}                          ← SP 恢复，PC 拿回返回地址
```

### PC 的变化：什么是硬件自动，什么是指令驱动

| PC 的变化 | 谁驱动 |
|---|---|
| **顺序执行 +2 / +4** | **硬件自动**（Thumb 指令 2 或 4 字节） |
| **跳转 / 返回** | **指令驱动** —— Flash 里躺着 `bl` / `bx lr` / `pop {pc}` |
| **复位、中断** | **硬件自动** —— 从向量表取地址装进 PC，不需要指令 |

「函数运行完 PC 跳回去」**不是硬件自发的**，是因为函数末尾有一条真实的返回指令。

> 这正是那次 HardFault 的成因：残缺的 `_init` 少了 `bx lr`，PC 就顺着往下掉进了下一个函数。
> 详见 `STM32HAL/DEBUG_NOTES.md` 问题 3。

### 直接调用 vs 间接调用

本工程实测：**89 条 `bl`**（目标编译期写死在机器码里）+ **2 条 `blx r3`**（目标运行期才知道）。

```
80001A8:  4798    blx r3      ← 在 __libc_init_array 里，调 .init_array 表里的函数指针
80001C4:  4798    blx r3
```

**间接调用的常见场景**：函数指针、HAL 回调（`HAL_UART_TxCpltCallback` 等）、中断向量表（硬件从表里读地址再跳）。

> `blx r3` 里 `r3` 是垃圾值时，CPU 照样忠实跳过去 —— 它无法判断一个地址"合理不合理"。
> 这就是为什么这类 bug 表现为 HardFault 而不是干净的报错。

---

## 七、没用到的函数**不占 Flash**

实测（用本工程同样的 `-ffunction-sections` + `--gc-sections`）：

```
编译后 .o 里：          f   never_called_global   used_fn
                       ↑ never_called_static 已经没了 —— 编译期就被删

链接 + --gc-sections：  f   used_fn                ← never_called_global 也被删
链接不加 --gc-sections：f   never_called_global    used_fn   ← 占 Flash @ 0x0800000C
```

**两级清理：**

1. **编译期** —— `static` 函数没人调用，编译器直接不生成（它确定文件外不可能有人用）
2. **链接期** —— 非 `static` 函数靠 `--gc-sections` 删掉；`-ffunction-sections` 让每个函数单独成段，才能逐个删

这就是 HAL 库那么大、本固件只有 5K 的原因：只调用了少数几个 HAL 函数，其余全被丢弃。

> **反面**：向量表没人"调用"，会被这套机制**误删** —— 必须 `KEEP(*(.isr_vector))` 保住。
> 同一个机制的两面，详见 `STMTest/LEARNING.md` 第 3 层的「`KEEP` 与 `--gc-sections`」。

---

## 常见误解速查

| ❌ 误解 | ✅ 实际 |
|---|---|
| 函数需要时被加载到 RAM 里执行 | Flash 原地执行（XIP）。`.text` 的 VMA==LMA，永不搬运 |
| `static` 决定变量/函数存在哪 | 只决定可见性（函数/全局）或存储期（局部）。位置由**初始化方式**决定 |
| static 局部变量一定进 RAM | `static const` 局部变量进 `.rodata`(Flash) |
| 写了 static 局部变量就会占 RAM | 只读不改的会被常量折叠删掉 |
| 局部变量都在栈上 | 优先在寄存器；数组、取地址、寄存器不够时才落栈 |
| 20K RAM 是栈和堆共用 | 底部 120 B 是静态区，谁都不能碰；中间 19.9K 才是栈堆共用 |
| 初始化的数据放进堆 | 放进**静态区**。堆只装 `malloc` 的东西，本工程堆 = 0 |
| 剩余 RAM 被"分配"给栈和堆 | 没有分配动作。栈从顶往下、堆从底往上，各自蚕食，无边界无仲裁 |
| 静态区有固定大小 | 大小由代码里的全局/static 变量决定，链接期定，运行期不变 |
| 链接器会保证栈够用 | 只在 `.data+.bss` 超总量时报错。留给栈多少它不管 |
| **SP 是下一条指令的地址** | **PC 才是。** SP 是栈顶地址，指向 RAM |
| SP 由硬件自动更新 | 主要靠指令（`push`/`sub sp`）。硬件只在**复位**和**进出异常**时动 SP |
| 函数执行完 PC 跳到下一个函数 | 回到**调用者** `bl` 的下一条指令 |
| `bl` 调用函数会动 SP | 只改 PC 和 LR。SP 由被调函数自己按需移动 |
| 函数返回是硬件自动的 | 靠一条真实的返回指令（`bx lr` / `pop {pc}`）。少了它 PC 会顺着往下掉 |
| 写了函数就会占 Flash | 没调用的会被删：static 编译期删，非 static 靠 `--gc-sections` 删 |
| 跳转目标都是编译期确定的 | 直接 `bl` 是；`blx r3`（函数指针、回调、中断向量）运行期才知道 |

---

## 复现本文所有数据的命令

```bash
cd ~/STMPrj/STM32HAL

# 段的 VMA / LMA（判断要不要搬运）
arm-none-eabi-objdump -h build/firmware.elf

# 符号的地址、类型（大小写 = 全局/局部）、所在段
arm-none-eabi-nm -n build/firmware.elf
arm-none-eabi-objdump -t build/firmware.elf

# RAM 布局的边界符号
arm-none-eabi-nm build/firmware.elf | grep -E '_s?(data|bss|idata)|_e(data|bss)|_estack'

# 单个函数的反汇编（看栈帧、字面量池、bl/blx）
arm-none-eabi-objdump -d build/firmware.elf --disassemble='main'

# 看某个地址上的原始字节
arm-none-eabi-objdump -s -j .text --start-address=0x800050c --stop-address=0x8000510 build/firmware.elf
```

> **看产物比看源码可靠。** 编译器会删变量、把局部变量放寄存器、删未调用的函数 ——
> 源码写了不代表最终存在。
